from __future__ import annotations

import pandas as pd


def example_python_metric(df_matches: pd.DataFrame) -> pd.DataFrame:
    """Example of building a metric in Python if needed."""
    return df_matches.head(0)

